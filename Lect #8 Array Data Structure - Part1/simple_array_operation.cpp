#include <iostream>
using namespace std;
int main() {
    int arr[] = {1, 2, 3, 4, 5}; 
     cout<<arr[2];  /* print any value from the array using the index of array.
     For example if array index = 0 ---> it will print 1st value given inside array...  the value can be any number or othrr data type
     but the array index always starts from 0. 
     That's why when we read loops we have be told to adopt the writing of loops value starting from no 0 */
     return 0;
}



